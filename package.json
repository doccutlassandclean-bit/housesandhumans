{
  "name": "housesandhumans",
  "version": "0.1.0",
  "private": true,
  "description": "Houses & Humans — lightweight AI Dungeon Master (Phase 1: backend handoff)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --watch --env-file-if-exists=.env server.js",
    "test": "node test/phase1.test.js && node test/premise.test.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "express": "^4.21.2"
  },
  "allowScripts": {
    "better-sqlite3@12.11.1": true
  }
}
